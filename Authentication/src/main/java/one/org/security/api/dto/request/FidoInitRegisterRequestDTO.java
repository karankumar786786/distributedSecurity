package one.org.security.api.dto.request;

import lombok.Data;

@Data
public class FidoInitRegisterRequestDTO {
    // Currently empty, but reserved for future flags (e.g. authenticator selection
    // criteria)
}
